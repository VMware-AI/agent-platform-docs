# CHANGELOG

本文件记录**文档站自身**的版本变更。

> **版本号的含义 —— 两个版本各管各的**
>
> - **文档站版本**（`package.json` / 离线包文件名）—— 本手册这套站点的迭代版本
> - **平台版本**（站点导航右上角显示的 `v0.0.1`）—— 手册所描述的 agent-platform 版本
>
> 两者独立：平台没发新版时，手册也可能因为补充内容而发新版。

---

## 0.0.2

对应平台版本：**v0.0.1**

补齐了四块此前手册没有覆盖的功能，并把各页的字段、枚举与操作路径按后端实现重新核对了一遍。全站 **42 页**。

### 新增页面

| 页面 | 内容 |
|---|---|
| 控制台 → **配置与知识包** | 智能体配置与 OKF 知识包挂载：制品四种类型、sha256 三重校验 + 防路径穿越解压 + 原子切换、同步失败 best-effort 不影响心跳。并注明 v0.0.1 侧栏未暴露入口 |
| 参考 → **日常运维手册** | 按「我要做什么」组织的 SOP：交付 / 变更 / 排查 / 对账 / 维护五类，含巡检节奏表 |
| 参考 → **安全基线** | 分档可勾选清单（装完当天 / 第一周 / 例行）、平台自带的安全设计、七条常见错误做法、事件响应六步止血顺序 |
| 参考 → **规格与限制** | 命名规则、密码策略、分页与导出上限、各类枚举、资源门槛、需输入名称确认的操作一览 |

### 补充的功能细节

- **VM 管理台密码默认 30 天自动轮换** —— 心跳时检查上次轮换时间，超期排队一条命令，同一时刻只允许一条在飞
- **「密码更新」与「密钥轮换」的区别** —— 前者换 OS 登录口令，后者换管理台凭据
- **导出上限 5000 条**（内部每批 500）—— 请求日志、审计日志
- **会话行为** —— httpOnly cookie、默认 8 小时 TTL、「记住我」的实际语义
- **界面基础** —— 中英文切换、深浅主题、侧栏折叠
- **后端已有、控制台未暴露的能力** —— 自定义角色 / 权限点、部门与成员模型；并解释了计量的「按部门」维度为何常为空

### 修正

- 快照能力：后端支持、**控制台 v0.0.1 未暴露按钮**，需要时在 vCenter 侧操作
- 请求日志 `detail` 字段的实际含义，去掉了一处无法从代码佐证的隐私断言

---

## 0.0.1

对应平台版本：**v0.0.1**

首个版本。基于 VitePress 的在线手册，构建产物为纯静态文件，可直接由 nginx 托管。

### 内容（38 页）

| 分组 | 页面 |
|---|---|
| 开始使用 | 平台简介 · 架构与组成 · 核心概念 · 快速开始 |
| 安装部署 | 环境要求 · 离线安装 · 首次登录 · 网络与证书 · 健康检查 · 升级与卸载 · 故障排查 |
| 控制台 | 总览仪表盘 · 初始化四步走 · 资源池接入 · 模型网关接入 · 用户与权限 · 平台设置 · 模型管理 · 密钥管理 · 网关路由 · 智能体市场 · 部署智能体 · 智能体实例 · 技能管理 |
| 可观测性 | 计量中心 · 计量设置 · 实时监控 · 请求日志 · 审计日志 |
| 智能体 VM | 访问智能体 · VM 自服务管理台 · 升级与回滚 |
| 参考 | 角色与权限矩阵 · 环境变量 · 端口与网络 · 离线部署本手册 · 常见问题 |

### 事实来源

内容与实现对齐，而不是照抄仓库文档：

- 安装与环境变量以 **release tarball 内的实际脚本**为准
- 控制台字段对齐 `agent-platform-console` 的 i18n
- 权限矩阵对齐 `agent-platform-backend` 的 RBAC
- VM 侧接口对齐 `agent-manager-daemon` 的 OpenAPI

据此记录了三处与仓库文档不一致的实际行为：**8 个容器**（含 otel-collector）、**console 443/80 硬编码**（`CONSOLE_PORT` 只影响横幅文案）、**`ADMIN_BOOTSTRAP_PASSWORD` 默认 `ChangeMe123!` 不会自动重生成**。

### 交付方式

- 在线站点（`npm run dev` / 构建后交 nginx）
- **离线发布包** —— 解压后 `sudo ./install.sh` 部署到自己的 nginx，目标机只需 nginx，全站含搜索可离线使用
- GitHub Actions：push / PR 构建（兼做死链检查），打 tag 自动出包挂 Release
